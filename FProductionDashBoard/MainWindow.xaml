<Window x:Class="FProductionDashBoard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FProductionDashBoard"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <ResourceDictionary Source="Themes/CardStyles.xaml"/>
    </Window.Resources>
    
    <StackPanel Margin="20">
        <!-- 總計資訊 -->
        <TextBlock Text="{Binding TotalDevices, StringFormat=設備總數: {0}}" 
                   FontSize="16" Margin="0,0,0,10"/>
        <TextBlock Text="{Binding TotalButtonClicks, StringFormat=按鈕總點擊次數: {0}}" 
                   FontSize="16" Margin="0,0,0,20"/>
        <!-- 新增設備按鈕 -->
        <Button Content="新增設備" Command="{Binding AddDeviceCommand}" Width="100" Margin="0,0,0,20"/>
        <!-- 設備卡片清單 -->
        <ItemsControl ItemsSource="{Binding Devices}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:DeviceCard Style="{StaticResource MainCardStyle}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</Window>
