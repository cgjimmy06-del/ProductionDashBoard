<UserControl x:Class="FProductionDashBoard.UserControls.ProgramDeviceCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FProductionDashBoard"
             mc:Ignorable="d"
             d:DesignHeight="160" d:DesignWidth="160">
    <Border Width="100" Height="100"
            CornerRadius="12"
            Background="{DynamicResource PrimaryBackgroundBrush}"
            Margin="6">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
        </Border.Effect>

        <Grid>
            <!-- 右上角燈號徽章 -->
            <Ellipse Width="12" Height="12"
                     HorizontalAlignment="Right"
                     VerticalAlignment="Top"
                     Margin="0,4,4,0"
                     Fill="{Binding Light, Converter={StaticResource ProgramLightToBrushConverter}}"/>

            <!-- 設備名稱（頂部） -->
            <TextBlock Text="{Binding DeviceName}"
                       FontSize="16"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       Margin="0,18,0,0"
                       TextTrimming="CharacterEllipsis"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>
            
            <!-- 主體：可生產數 + 分母（左右排列） -->
            <StackPanel Orientation="Horizontal" Margin="0,32,0,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding FeasibleLabel}"
                           FontSize="32"
                           FontWeight="Bold"
                           VerticalAlignment="Bottom"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock Text="{Binding TotalLabel}"
                           FontSize="16"
                           VerticalAlignment="Bottom"
                           Margin="2,0,0,3"
                           Opacity="0.7"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
