<UserControl x:Class="FProductionDashBoard.UserControls.HardwareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:localuc="clr-namespace:FProductionDashBoard.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <TabControl Style="{StaticResource MaterialDesignFilledTabControl}" Margin="4">

        <TabItem>
            <TabItem.Header>
                <StackPanel Orientation="Horizontal" Margin="4,0">
                    <materialDesign:PackIcon Kind="CreditCardScan"
                                             Width="20" Height="20"
                                             VerticalAlignment="Center"
                                             Margin="0,0,6,0"/>
                    <TextBlock Text="{DynamicResource HardwareTabCardReader}" VerticalAlignment="Center"
                               Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                </StackPanel>
            </TabItem.Header>
            <localuc:CardReaderSettingView DataContext="{Binding CardReader}"/>
        </TabItem>

        <TabItem>
            <TabItem.Header>
                <StackPanel Orientation="Horizontal" Margin="4,0">
                    <materialDesign:PackIcon Kind="RobotIndustrial"
                                             Width="20" Height="20"
                                             VerticalAlignment="Center"
                                             Margin="0,0,6,0"/>
                    <TextBlock Text="{DynamicResource HardwareTabAbbRobot}" VerticalAlignment="Center"
                               Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                </StackPanel>
            </TabItem.Header>
            <localuc:AbbRobotSettingView DataContext="{Binding AbbRobot}"/>
        </TabItem>

    </TabControl>
</UserControl>
